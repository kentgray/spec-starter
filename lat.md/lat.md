This directory documents the design intent, workflow, and command specifications for the spec-starter project using [lat.md](https://www.npmjs.com/package/lat.md). Install with `npm i -g lat.md`.

- [[workflow]] — The 4-step spec-driven development loop, spec format, and the design rationale for each step
- [[commands]] — Specification for each slash command: inputs, outputs, state transitions, and edge cases
